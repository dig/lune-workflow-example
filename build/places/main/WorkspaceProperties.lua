local WorkspaceProperties = {}

function WorkspaceProperties.SetProperties(workspace)
    print(`Setting properties for: {workspace.Name}`)

    -- workspace.StreamingEnabled = true
end

return WorkspaceProperties